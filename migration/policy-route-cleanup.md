# Title-only policy route cleanup

Drew approved removing only `/privacy-policy-2/` and `/terms-of-service-2/`. Both were title-only shells with no policy body and no inbound link from another published page.

The routes are excluded from capture publication, validation, browser audits, the generated sitemap, and `out/`. Raw inventory and browser-audit records remain as migration evidence. No other policy or template file was changed.
